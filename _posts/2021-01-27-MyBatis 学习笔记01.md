---
title: MyBatis 基于映射文件的基本使用
indextitle: MyBatis 学习笔记(一)
date: 2021-01-27 23:30:09
description: MyBatis 基于映射文件使用示例。创建数据库表对应的类，创建持久层接口，创建持久层映射文件，创建 MyBatis 配置文件配置数据库信息，指定持久层映射文件位置。持久层映射文件相当于持久层接口的实现类。
tags:
- MyBatis
---

创建数据库表和与数据库表对应的类，类的属性名与数据库表的字段名一致，类的属性类型与数据库表字段类型对应。表名与类名无需一致。

```
CREATE TABLE user (username VARCHAR(20), password VARCHAR(20));
```

```
public class User implements Serializable {

    private String username;
    private String password;
    省略 Get、Set 方法
}
```

创建持久层接口和持久层映射，持久层接口的名称必须与持久层映射文件名称保持一致，且持久层接口与持久层映射必须在相同的包路径下。持久层映射文件相当于持久层接口的实现类。

```
public interface IUserDao {
    查询用户信息
    public List<User> selectUserList();
}
```

```
<!-- 持久层映射文件文件名称为 IUserDao.xml -->
<mapper namespace="chu.yi.bo.dao.IUserDao">
    <select id="selectUserList" resultType="chu.yi.bo.domain.User">
        select * from user;
    </select>
</mapper>
```



创建配置文件

```
<!-- 创建配置文件，名称为 SqlMapConfig.xml(可任意取名) -->
<configuration>
    <!--  配置环境 -->
    <environments default="mysql">
        <!-- 配置 mysql 的环境 -->
        <environment id="mysql">
            <!-- 配置事务的类型 -->
            <transactionManager type="JDBC"/>
            <!-- 配置数据源（连接池） -->
            <dataSource type="POOLED">
                <!-- 配置连接数据库的 4 个基本信息 -->
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/test"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>
    </environments>

    <!-- 指定映射配置文件的位置 -->
    <mappers>
        <mapper resource="chu\yi\bo\dao\IUserDao.xml"/>
    </mappers>
</configuration>
```

```
public void testMyBatis() {
    读取配置文件
    InputStream in = Resources.getResourceAsStream("SqlMapConfig.xml");
    
    创建 SqlSession 对象
    SqlSessionFactoryBuilder builder = new SqlSessionFactoryBuilder();
    SqlSessionFactory factory = builder.build(in);
    SqlSession session = factory.openSession();
    
    使用 SqlSession 创建 dao 接口的代理对象
    IUserDao userDao = session.getMapper(IUserDao.class);
    使用代理对象执行查询操作
    List<User> users = userDao.selectUserList();
    for(User user : users) {
    	System.out.println(user);
    }
    
    释放资源
    session.close();
    in.close();
}
```

